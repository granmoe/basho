version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: sudo npm install -g npm@latest
      - run: sudo npm install -g now@latest
      - restore_cache:
          key: dependency-cache-{{ checksum "web/package.json" }}
      - run: |
          cd web
          npm install
      - save_cache:
          key: dependency-cache-{{ checksum "web/package.json" }}
          paths:
            - ./web/node_modules
      - run: |
          cd web
          npm test
      - deploy:
          command: |
              URL=$(now -t ${NOW_TOKEN})
              cd web
              now -t ${NOW_TOKEN} alias ${URL} basho.ai

# TODO: Don't care about this much right now, but we could get it working later
#      - run:
#          name: code-coverage
#          command: './web/node_modules/.bin/nyc report --reporter=text-lcov'
#      - store_artifacts:
#          path: test-results.xml
#          prefix: tests
#      - store_artifacts:
#          path: coverage
#          prefix: coverage
#      - store_test_results:
#          path: test-results.xml
